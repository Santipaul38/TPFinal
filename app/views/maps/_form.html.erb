<%= form_with(model: map, local: true) do |form| %>
  <% if map.errors.any? %>
    <div id="error_explanation">
      <h6 style="font-weight: bold;"><%= pluralize(map.errors.count, "error") %> prohibited this map from being saved:</h6>

      <ul>
        <% map.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  <br>
  <div class="field">
    <%= form.label :columns %>
    <%= form.text_field :columns %>
  </div>
  <br>
  <div class="field">
    <%= form.label :rows %>
    <%= form.text_field :rows %>
  </div>
  <br>
  <button onclick="<%form.submit%>" class="btn btn-outline-dark btn-lg">
        Create Map
  </button>
<% end %>
